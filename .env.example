# Server
HOST=127.0.0.1
PORT=8000
ALLOWED_ORIGINS=
MAX_JSON_BYTES=2097152
MAX_UPLOAD_BYTES=10485760
RATE_LIMIT_PER_MIN=120
NATURALIZER_STATE_DIR=state
NATURALIZER_PLAN=free

# Naturalizer intentionally runs locally and does not accept cloud-model or
# external-detector credentials.
